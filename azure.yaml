# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: what-to-eat-backend
metadata:
  template: azd-init@1.23.2
services:
  api:
    project: ./src/API
    host: appservice
    language: dotnet
